    You’re very close — the issue is mainly about **how redirect is handled + clearing it on logout**.

Right now:

* You correctly pass `?redirect=/project` when user is not logged in ✅
* After login → you navigate to that redirect ✅
* BUT after logout → that old redirect still exists in URL ❌

So it keeps reopening `/project` instead of default `/invoices`.

---

# 🔴 Root Cause

The problem is here:

```js
navigate(redirect);
```

You are **always using redirect from URL params**, even after logout.

And when you logout:

* URL is still like → `/signin?redirect=/project`
* So login again → goes to `/project` again

---

# ✅ Proper Fix (Clean Solution)

## ✅ 1. Fix Logout → Always go to clean signin URL

👉 In `UserProfilePopover.js`

Change this:

```js
navigate("/signin");
```

### ✅ Update to:

```js
navigate("/signin", { replace: true });
```

👉 OR even better (remove redirect completely):

```js
navigate("/signin", { replace: true, state: {} });
```

---

## ✅ 2. Clear redirect after successful login

👉 In your `Login.js`

Change this:

```js
navigate(redirect);
```

### ✅ Update:

```js
navigate(redirect, { replace: true });
```

👉 This removes query params from history

---

## ✅ 3. Fix ProtectedRoute (IMPORTANT ⚠️)

🚫 You are doing this (WRONG):

```js
return navigate(`/signin?redirect=${encodeURIComponent(location.pathname)}`)
```

👉 `navigate()` should NOT be returned inside render.

---

### ✅ Replace with:

```js
return (
  <Navigate
    to={`/signin?redirect=${encodeURIComponent(location.pathname)}`}
    replace
  />
);
```

---

## ✅ 4. Fix App.jsx logout redirect logic

This line is wrong:

```js
navigate(redirect);
```

👉 Because `redirect` may still be `/project`

---

### ✅ Replace with:

```js
navigate("/signin");
```

---

# 💡 Optional Improvement (Best Practice)

Instead of using query params, you can use **location state**

### In ProtectedRoute:

```js
return (
  <Navigate
    to="/signin"
    replace
    state={{ from: location.pathname }}
  />
);
```

---

### In Login:

```js
const location = useLocation();
const redirect = location.state?.from || "/invoices";
```

👉 This is cleaner & auto-clears after navigation

---

# ✅ Final Behavior After Fix

### Scenario:

1. User opens → `/project`
2. Redirected → `/signin?redirect=/project`
3. Login → goes to `/project` ✅
4. Logout → goes to `/signin` ✅ (NO redirect param)
5. Login again → goes to `/invoices` ✅

---

# 🔥 Summary

### You must:

✔ Use `<Navigate />` instead of `navigate()` in render
✔ Use `replace: true` in navigation
✔ Remove redirect param after login
✔ Always go to `/signin` clean on logout

---

If you want, I can refactor your whole auth flow (ProtectedRoute + App + Login) into a **clean production-ready version** — your current setup is good but slightly overcomplicated.
------------------------------------
Good question — I’ll give you **exact files + exact lines to change** so you don’t get confused.

---

# ✅ 1. **ProtectedRoute.js**

### 🔴 Problem:

You are using `navigate()` inside render (wrong)

### 🔧 Change THIS:

```js
return navigate(`/signin?redirect=${encodeURIComponent(location.pathname)}`)
```

### ✅ Replace WITH:

```js
return (
  <Navigate
    to={`/signin?redirect=${encodeURIComponent(location.pathname)}`}
    replace
  />
);
```

---

# ✅ 2. **Login.js (signin page)**

### 🔴 Problem:

Redirect param stays in URL history

### 🔧 Find:

```js
navigate(redirect);
```

### ✅ Replace WITH:

```js
navigate(redirect, { replace: true });
```

---

# ✅ 3. **UserProfilePopover.js (Logout button)**

### 🔴 Problem:

Old redirect param remains after logout

### 🔧 Find:

```js
navigate("/signin");
```

### ✅ Replace WITH:

```js
navigate("/signin", { replace: true });
```

---

# ✅ 4. **App.jsx (IMPORTANT ⚠️)**

### 🔴 Problem:

After logout, it uses old redirect param again

### 🔧 Find:

```js
navigate(redirect);
```

### ✅ Replace WITH:

```js
navigate("/signin");
```

---

# 🧠 Optional (Best Practice – Cleaner)

👉 Instead of query param (`?redirect=`), use **location.state**

---

## 🔁 ProtectedRoute.js

```js
return (
  <Navigate
    to="/signin"
    replace
    state={{ from: location.pathname }}
  />
);
```

---

## 🔁 Login.js

```js
import { useLocation } from "react-router-dom";

const location = useLocation();
const redirect = location.state?.from || "/invoices";
```

---

# 🎯 Final Checklist

| File                    | Change                                   |
| ----------------------- | ---------------------------------------- |
| ✅ ProtectedRoute.js     | replace `navigate()` with `<Navigate />` |
| ✅ Login.js              | add `{ replace: true }`                  |
| ✅ UserProfilePopover.js | fix logout navigation                    |
| ✅ App.jsx               | remove redirect usage on logout          |

---

# 🚀 After this fix

✔ Copy `/project` → opens after login
✔ Logout → goes to clean `/signin`
✔ Login again → opens `/invoices` (default)

---

If you want, I can also:
👉 simplify your whole auth flow (React Query + Redux + routing)
👉 or fix your invoice route/entity logic (you commented it out 👀)
------------------------------
Yes — I understood 👍
You don’t want it only for `/project`, you want it for **ANY protected route dynamically**.

Good news: your current approach already supports that — you just need to **fix implementation**, not logic.

---

# 🎯 What you actually want

### Scenario (for ANY route):

1. User opens → `/reports` OR `/records` OR `/invoices/123`
2. Not logged in → redirect to login
3. After login → go back to SAME page ✅
4. After logout → go to clean `/signin` (NO old redirect) ✅
5. Login again normally → go to `/invoices` (default) ✅

---

# ✅ Final Correct Implementation (Dynamic for ALL routes)

## ✅ 1. ProtectedRoute.js (MAIN LOGIC)

### 🔧 Replace your token check block with:

```js
const token = localStorage.getItem('auth_token');

if (!token) {
  return (
    <Navigate
      to="/signin"
      replace
      state={{ from: location.pathname }} // 🔥 dynamic route
    />
  );
}
```

👉 This automatically works for:

* `/project`
* `/reports`
* `/anything`
  No need to hardcode anything ✅

---

## ✅ 2. Login.js (Use dynamic redirect)

### 🔧 Add:

```js
import { useLocation } from "react-router-dom";
```

### 🔧 Then:

```js
const location = useLocation();
const redirect = location.state?.from || "/invoices";
```

---

### 🔧 Replace:

```js
navigate(redirect);
```

### ✅ With:

```js
navigate(redirect, { replace: true });
```

---

## ✅ 3. Logout (UserProfilePopover.js)

### 🔧 Replace:

```js
navigate("/signin");
```

### ✅ With:

```js
navigate("/signin", { replace: true });
```

👉 This ensures:

* Old route is NOT remembered
* No redirect loop

---

## ✅ 4. App.jsx (IMPORTANT CLEANUP)

### 🔴 REMOVE this logic:

```js
const redirect = searchParams.get('redirect')
```

AND

```js
navigate(redirect);
```

---

### ✅ Replace logout fallback with:

```js
navigate("/signin");
```

---

# 🔥 Why this is BEST approach

| Method                       | Problem                    |
| ---------------------------- | -------------------------- |
| ❌ Query param (`?redirect=`) | stays in URL → causes bugs |
| ✅ location.state             | temporary → auto cleared   |



